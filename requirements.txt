# OS Screen Observer - Dependencies
# Install with: pip install -r requirements.txt
#
# Platform notes:
#   Windows: pywinauto + pywin32 + psutil provide full UIA support
#   macOS:   mss + pyobjc (optional; AX tree currently stub)
#   Linux:   mss + wmctrl (wmctrl must be installed via apt/brew; AT-SPI tree currently stub)
#   All:     --mock flag runs without any OS-specific deps

# Core (all platforms)
flask>=3.0.0
flask-cors>=4.0.0
mss>=9.0.0
Pillow>=10.0.0
# VLM is reached through any OpenWebUI-compatible OpenAI chat-completions
# endpoint via stdlib `urllib` — no provider SDK is required here.

# OCR (optional but recommended) — also install the Tesseract binary itself:
#   Windows: https://github.com/tesseract-ocr/tesseract/releases  +  in
#            config.json set ocr.tesseract_cmd to its full path; backslashes
#            inside JSON strings must be escaped (use "\\" or forward slashes).
#   macOS:   brew install tesseract
#   Linux:   sudo apt install tesseract-ocr
pytesseract>=0.3.10

# Input automation / actions (all platforms)
pyautogui>=0.9.54

# Windows-specific (skip on macOS/Linux)
pywinauto>=0.6.8; sys_platform == "win32"
pywin32>=306; sys_platform == "win32"
psutil>=5.9.0; sys_platform == "win32"

# Scenario DSL (mock-mode scripted state machines)
PyYAML>=6.0

# Optional accessibility-tree adapters (auto-detected at runtime)
# macOS:  pyobjc-core, pyobjc-framework-Cocoa, pyobjc-framework-Quartz
# Linux:  python3-atspi (system package; or `pip install pyatspi` where available)

# Development / testing — see requirements-dev.txt
